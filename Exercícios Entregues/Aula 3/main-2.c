#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int a, b, c;
	a=5; // = operador de associação
	b=4;
	c = a + b;
	// Variável é composta por tipo primitivo, identificador e valor
	int x = 6;
	// printf exibe uma saída
	printf ("O valor = %d", c);
	
	return 0;
}
