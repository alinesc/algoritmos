#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
	int i = 0, a, b, r;
	while (i < 20)
	{
	scanf("%d", &a);
	scanf("%d", &b);
	r = a + b;
	printf("%d\n", r);
	i++;
}
	
	return 0;
}
